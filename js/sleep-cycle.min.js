export const transformTime=quantidade=>{let time;return new Date(6e4*calcularQuantidadeCicloSono(quantidade)).toISOString().substr(11,5)};const calcularQuantidadeCicloSono=qtd=>{let cicle=90;return 90*qtd};export const calcTime=quantidade=>{const starOrEndSleep=timeStartProcess(),quantidadeDormida=calcularQuantidadeCicloSono(quantidade),Radio=document.querySelectorAll('[type="radio"]');return Radio[0].checked?starOrEndSleep.setMinutes(starOrEndSleep.getMinutes()+quantidadeDormida):starOrEndSleep.setMinutes(starOrEndSleep.getMinutes()-quantidadeDormida),starOrEndSleep.toISOString().substr(11,5)};const timeStartProcess=()=>{const starOrEndSleep=document.querySelector("#start-sleep").value,timeArray=tratamentoTime(starOrEndSleep);let h=parseInt(timeArray[0]),m=parseInt(timeArray[1]);const date=new Date(0);return date.setHours(date.getHours()+h),date.setMinutes(date.getMinutes()+m),date},tratamentoTime=time=>{let valores;return time.split(":")};